package modificadores;

public class Static {

    //Static: É usado para criação de uma variável que podeserá ser acessada por todas
    // instâncias de objetos dessa classe como uma variável comum.
    // A variável criada será a mesma em todas as instâncias, quando o seu conteúdo é modificado
    // numas das instãncias, a modificação é propagada em toda as demais instâncias
    // Nas declarações de métodos ajudam no acesso direto á classe, não é necessário
    // estanciar um objeto para acessar o método.

}
